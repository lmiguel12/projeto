@model IEnumerable<WebMVC.Models.Filiacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cidade.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cidade.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FiliacaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FiliacaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FiliacaoId })
        </td>
    </tr>
}

</table>
